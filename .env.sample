# BioLog 環境変数サンプル
# このファイルをコピーして `.env` を作成し、必要な値を設定してください。
# `.env` は .gitignore で除外されます。

# データ保存ディレクトリ（デフォルト: ./data）
# 既存の DB を別の場所に持っているならフルパスを指定
# 例（Windows）: BIOLOG_DATA_DIR=D:/AI/biolog/data
# 例（Linux）:   BIOLOG_DATA_DIR=/var/lib/biolog
BIOLOG_DATA_DIR=./data
